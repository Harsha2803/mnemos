You are picking up Mnemos at:

  /home/shreeharsha/Personal/Projects/Resume_001/mnemos

Current date of this handoff: 2026-08-17.

Start from `main`.

Read first, in order:

  1. TRACKER.md — it is the live source of truth. Read the header, the 2026-08-17
     `B1` deliverable-5 note near the top, §3's `B1` entry, and §5's "Then, in
     order" list.
  2. docs/ADAPTATION.md — read §7's `B1` row, §8's `B1` subsection, and the
     "Not started" list.
  3. Only then read code for the next milestone.

Current state:

  - `A0` through `A3` are done and merged to `main`.
  - `B1` is done and merged to `main`: source connectors, Redis Streams,
    authenticated ingestion WebSocket, the worker's real connector-ingest path,
    backend connector routes, the Sources UI, local-fs fixture wiring, and the
    Playwright sources spec.
  - The out-of-band frontend polish PR #18 is also merged to `main`: conversation
    naming, rename/delete, upload limits and multi-file upload, chat layout
    padding, resizable panels, sidebar collapse, hover marquee titles, motion
    polish, and the transcript scrollbar.
  - The per-session log-file work from PR #17 is already merged.
  - As of this handoff there should be no open PRs. If `gh pr list --state open`
    says otherwise, trust the live output and inspect that PR before starting
    new work.

Verification evidence for completed `B1`:

  - `docker compose up -d --build` rebuilt the stack.
  - `curl localhost:8000/readyz` returned `postgres`, `redis`, `ollama`, and
    `objectstore` all `ok`.
  - In Chromium, signed in as org `mnemos`, user `analyst@mnemos.local` /
    `analyst`, opened Sources, registered a `local_fs` connector against
    `/fixtures/sources`, browsed to `handbook.txt`, selected and ingested it, and
    watched the live feed reach `Succeeded` without a page reload.
  - `cd frontend && npx playwright test e2e/sources.spec.ts` passed; a separate
    headed Chromium run of the same spec also passed.
  - After merging PR #18 first and resolving `B1` against the updated `main`:
    backend `make test` — 429 passed; `make lint`, `make types`, and `make check`
    clean.
  - Frontend after the same resolution: `npm run lint`, `npx tsc --noEmit`,
    `npm run test` — 111 passed, and `npm run build` clean.
  - The rebuilt combined compose stack reported `/readyz` fully green and
    `npx playwright test e2e/sources.spec.ts` passed again.

Important rules:

  - Never use the work GitHub account or email (`harshaJKT`, `@jktech.com`).
    Confirm `Harsha2803` before any push.
  - `.claude/` is local tooling state and must not be committed.
  - Do not read or copy from `../jiva/`; it is the author's employer's proprietary
    codebase.
  - Do not run `pkill -f mnemos`; it can match the agent shell. Kill by port or PID.
  - Zero monetary cost forever: free, self-hosted defaults; Ollama is the default
    model path; no paid cloud pub/sub.

What is next:

  - The next milestone is `B2`, not `A4`: heartbeat, retries, richer status
    history, stuck-job surfacing, replay/progress depth, and per-job progress UI
    over the job machinery `B1` introduced.
  - Ordinary one-task-per-session discipline is back in force. Do not start `B2`
    in the same session that only intended to merge cleanup PRs.
