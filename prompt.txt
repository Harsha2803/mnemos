You are picking up Mnemos at:

  /home/shreeharsha/Personal/Projects/Resume_001/mnemos

Current date of this handoff: 2026-08-18.

Repository state at this handoff:

  - `A4` is merged to `main` in PR #20. Phase A, `B1`, and `B2` are complete.
  - The roadmap has been deliberately narrowed for a resume-focused portfolio finish.
    Only `B3`, `C4`, and reduced `D1` remain committed; `B4` and `C1`–`C3` are
    deferred unless the owner explicitly reopens them after `D1`.
  - The documentation-only scope reset is on `agent/resume-focused-roadmap`, PR #21. If
    that PR is still open, finish its repository lifecycle before starting implementation.
    If live GitHub state says it is merged, trust it and start from clean `main`.

Read first, in order:

  1. TRACKER.md — it is the live source of truth. Read the header, §0's agent
     operating instructions, the 2026-08-18 scope-reset note, §3.0's milestone ledger,
     §5's complete `B3` brief, and §7's update protocol.
  2. docs/ADAPTATION.md — read §1, §3, §7, the committed-work/deferred section in
     §8, and §10's notes for the next session.
  3. docs/Roadmap.md and docs/ImplementationPlan.md — these define why the finish line
     is narrow and what `B3` → `C4` → reduced `D1` means.
  4. README.md, then the existing tool models and auth/router/chat code named by
     TRACKER §5.
  5. docs/CodingStandards.md before backend work and docs/DesignSystem.md before
     frontend work.

Current product state:

  - `A0` through `A4`, `B1`, and `B2` are done and merged.
  - Users can authenticate, stream chat, upload and query documents with citations,
    query the demo warehouse through guarded NL2SQL, and ask without choosing a mode.
  - Source connectors feed Redis Streams jobs with heartbeats, retry/backoff, status
    history, stuck-job surfacing, replay hydration, and per-job progress.
  - Postgres RLS is forced for tenant data; retrieval authorization is applied inside
    the scan; NL2SQL retains both the AST read-only guard and `mnemos_ro` execution.
  - The default stack remains free and self-hosted: Ollama, Postgres/pgvector, Redis,
    MinIO, and Keycloak. No paid account is required.

Portfolio-focused remaining plan:

  1. `B3` — register and safely call one self-hosted MCP tool with per-user
     credentials, live-role/trust-tier authorization, durable approval, invocation
     history, a Tool console, and a denial naming the offending retrieved source.
  2. `C4` — port bitemporal memory and the budgeted context compiler to Postgres,
     expose admissions/exclusions/token spend in the inspector, and re-run the benchmark.
  3. Reduced `D1` — prove clean-clone Compose, cover critical journeys in Playwright,
     add a deterministic demo walkthrough, and rewrite the README around reproducible
     measurements and honest limitations.

Deliberately deferred:

  - `B4`: multi-step plans, loops, checkpoints, crash recovery, and replay.
  - `C1`: API keys, exhaustive RBAC permission matrix, and tag-scoped ACL UI.
  - `C2`: versioned prompt management and cost dashboard.
  - `C3`: folders, bookmarks, feedback, history search, and expanded audit UI.

Important rules:

  - Never use the work GitHub account or email (`harshaJKT`, `@jktech.com`).
    Confirm `Harsha2803` before every push or PR mutation.
  - `.claude/` is local tooling state and must not be committed.
  - Do not read or copy from `../jiva/`; it is the author's employer's proprietary
    codebase.
  - Do not run `pkill -f mnemos`; it can match the agent shell. Kill by port or PID.
  - Preserve zero-cost, self-hosted defaults and all existing security invariants.
  - Cut optional surface area before cutting typing, tests, error handling, or evidence.
  - Every backend capability ships with its usable frontend slice.
  - Apply TRACKER §0 and §7 at the end of every task: full verification,
    synchronized TRACKER/ADAPTATION/roadmap/handoff, scoped conventional commits,
    personal-account push, green PR, ready/merge for completed milestone work, then
    sync and verify clean `main`.

What is next after the documentation PR is closed:

  - Complete `B3` and stop before `C4`.
  - Follow TRACKER §5's five deliverables in order. Reuse the existing MCP tables,
    keep credentials per user, authorize again at the invocation boundary using the
    motivating trust tier, and name an offending retrieved source in a denial.
  - `B3` is single-call only. Do not start deferred `B4` work.
